fastapi<0.115.99
starlette<1.0.0
uvicorn
httpx
celery
redis
# Pinned below 0.0.40: from there on a bare `datetime` field maps to SQLModel's
# UTCDateTime, which refuses naive values on write. This codebase stores naive
# UTC throughout (datetime.utcnow()) against `timestamp without time zone`
# columns, so an unpinned upgrade breaks every timestamp write -- it took prod
# down on 2026-09-22. Lifting this pin means migrating the models and the
# columns to timezone-aware datetimes first; see tests/test_naive_datetime_persistence.py.
sqlmodel<0.0.40
psycopg2-binary
pydantic-settings
requests
dnspython
playwright
beautifulsoup4
jinja2<3.1.5
python-multipart>=0.0.22
aiofiles

ipwhois
fpdf2
pandas
openpyxl
python-whois
Pillow>=12.1.1
imagehash
tldextract
PyJWT>=2.10.1
cryptography>=46.0.5
passlib[bcrypt]
bcrypt==3.2.2
pyotp
markdown
bleach
croniter==6.0.0
networkx
matplotlib
psutil

# Metrics
prometheus-client>=0.19.0
mmh3==4.1.0
defusedxml>=0.7.1
