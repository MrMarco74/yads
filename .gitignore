logs/
yads/api/static/screenshots/

# IDE / agent tool local config — never commit
.vscode/
.agent/
.continue/
# tests/ — removed from ignore so the test suite is tracked in git
releases/
release_build/
product_screenshots/
.venv/
venv/
__pycache__/
node_modules/

# Environment & Configuration Files
*.env
*.env.*
.env.local
.env.production
data/config.env
data/*.env

# Database Backups & Dumps
*.sql
*.sql.gz
*.db
*.sqlite
*.sqlite3
support_backups/
logs/backups/

# Backup Files
*.bak
*.backup

# Private keys — never commit
*.pem
*.key

# LLM GUI
.llmgui.context
.llmgui_db/
.llmgui_history

# Debug
debug.log

# Tests
tests/results/

# Runtime scan artifacts — must NOT be in git or Docker images
yads/api/static/screenshots/
yads/api/static/screenshots_locked/

# Test backups
tests/*.zip
tests/*.sql

# Security scan results
results_semgrep.txt
results_trivy.txt
results.trivy
trivy.txt
ergebnisse.txt
