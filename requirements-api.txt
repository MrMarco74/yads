# DEPRECATED / UNUSED as of 2026-08-25 — no longer referenced by the Dockerfile.
# The api and worker images now BOTH install the full requirements.txt (only the
# heavy binaries — Chromium, Nuclei, Nmap — remain worker-only), because a slim
# api dependency subset caused ModuleNotFoundError crash-loops whenever the API
# loaded any code that imported a scanner module (the bs4 incident). Kept only
# for historical reference; edit requirements.txt instead.
#
# API-only dependencies — used by the FastAPI server container.
# Worker-only packages (playwright, imagehash, mmh3, ipwhois, python-whois,
# Pillow) are intentionally excluded; see requirements.txt for the
# full set used by the Celery worker image.

fastapi<0.115.99
starlette<1.0.0
uvicorn
httpx
celery
redis
sqlmodel
psycopg2-binary
pydantic-settings
requests
dnspython
jinja2<3.1.5
python-multipart>=0.0.22
aiofiles
tldextract

# Reports & exports (served by the API)
fpdf2
pandas
openpyxl

# Graph generation (served by the API)
networkx
matplotlib

# Auth
PyJWT>=2.10.1
cryptography>=46.0.5
passlib[bcrypt]
bcrypt==3.2.2
pyotp

# Misc
markdown
bleach
croniter==6.0.0

# Metrics & system monitoring
prometheus-client>=0.19.0
defusedxml>=0.7.1
psutil
