server {
    listen 80;
    server_name dsgvo.testlab.local;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Intentionally missing security headers and privacy policy
}
